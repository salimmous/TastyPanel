route:
  receiver: "default"
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 2h

receivers:
  - name: "default"
    webhook_configs:
      - url: "http://host.docker.internal:8080/alerts"
        send_resolved: true

